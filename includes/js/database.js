var database = [];
function createUser (fullName, email,date){
 profile = {
 	name : fullName,
 	email : email,
 	date : date
	};
}

$(document).ready(function() {
  $('table.highchart').highchartTable();
});